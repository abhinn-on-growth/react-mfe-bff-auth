{
  "name": "module-federation-auth-home-dashboard",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @app/host\" \"npm run dev --workspace @app/home-dashboard\" \"npm run dev --workspace @app/auth\"",
    "build": "npm run build --workspaces --if-present",
    "preview": "npm run preview --workspace @app/host"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
